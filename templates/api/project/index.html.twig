{% extends 'base.html.twig' %}

{% block title %}List Projects{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="text-danger">Page List Loaded: {{ date.format('Y-m-d H:i:s') }}</h1>
        <div class="mt-5">
            <a href="{{ path('project_list_react') }}" role="button" class="btn btn-primary">
                Projects
            </a>
            <a href="{{ path('project_create_page') }}" role="button" class="btn btn-success">
                Create Project
            </a>
        </div>
        <div {{ react_component('ListComponent') }}></div>
    </div>
{% endblock %}